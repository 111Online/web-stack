@using NHS111.Models.Models.Web
@inherits NHS111.Web.Views.Shared.SurveyLinkView<OutcomeViewModel>
@{
    Layout = "~/Views/Shared/_LayoutNhsUk.cshtml";
}

@section Title {
    NHS 111 - Thanks for providing your details
}

@section HeadTop {
    @{ var url = string.Format("/outcome/{0}/{1}/{2}/disposition/", Url.Encode(Model.PathwayNo), Url.Encode(Model.OutcomeGroup.Text), Url.Encode(Model.Id)); }
    <script>
        var pageId = '@Model.Id';
        dataLayer = [
        {
            'virtualPageUrl': '@url',
            'virtualPageTitle': '@Model.TitleWithoutBullets'
        }];
    </script>
}


@Html.HiddenFor(x => Model.Id)

<div class="measure nhsuk-core">

    <h1 class="nhsuk-heading-xl">
        Thanks for providing your details.
    </h1>

    <div class="callout callout--attention nhsuk-u-margin-bottom-6">
        <p>
            Your information will only be shared within the NHS.
        </p>
        <p>
            You can read more details in this
            <a href="#" data-event-trigger="click" data-event-value="SMS privacy policy" target="_blank">
                privacy policy (opens in a new tab or window)
            </a>
        </p>
    </div>
</div>