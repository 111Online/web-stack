@using System.Web.Mvc.Html
@using NHS111.Models.Models.Web
@inherits NHS111.Web.Views.Shared.SurveyLinkView<SendSmsOutcomeViewModel>
@{
    Layout = "~/Views/Shared/_LayoutNhsUkSiteStyle.cshtml";
}
@section Title {
    There's been a problem
}

<div class="measure nhsuk-core">

    <h1 class="nhsuk-heading-xl">
        There's been a problem
    </h1>

    <p>Sending your information failed.</p>

    <hr class="nhsuk-section-break--visible nhsuk-u-margin-top-6 nhsuk-u-margin-bottom-6" />

    @using (Html.BeginForm("CheckDetailsSMS", "Outcome", FormMethod.Post, new { id = "submitDetailsForm" }))
    {
        @Html.Partial("_CommonHiddenFieldsOutcomeViewModel", Model)
        @Html.Partial("_CommonHiddenFieldsCareAdviceViewModel", Model)
        @Html.Partial("_commonHiddenFieldsSendsSMSViewModel", Model)
        @Html.HiddenFor(m => m.SelectedServiceId)
        <button id="book-call" class="button--link nhsuk-back-link__link nhsuk-u-margin-bottom-6">
            <svg class="nhsuk-icon nhsuk-icon__chevron-left" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M8.5 12c0-.3.1-.5.3-.7l5-5c.4-.4 1-.4 1.4 0s.4 1 0 1.4L10.9 12l4.3 4.3c.4.4.4 1 0 1.4s-1 .4-1.4 0l-5-5c-.2-.2-.3-.4-.3-.7z"></path>
            </svg>
            Try again
        </button>
    }
</div>